# Constitution — redirect

The full project constitution lives in **[CONSTITUTION.md](CONSTITUTION.md)**.

This file is preserved as a redirect stub for two reasons:
1. Older agent guidance and tool integrations referenced
   `Constitution.md` (mixed case) before consolidation.
2. On case-insensitive filesystems (macOS HFS+ default, Windows NTFS
   default) only one of the two filenames can exist. On case-sensitive
   filesystems (Linux ext4, this project's primary host) both files
   resolve to the same content via this redirect.

If you are looking for project rules, the Four Gates, CONST-033, the
Universal Mandatory Constraints, or the Definition of Done — open
**[CONSTITUTION.md](CONSTITUTION.md)**.
